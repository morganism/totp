# frozen_string_literal: true

source 'https://rubygems.org'

# Core TOTP (totp.rb) needs ONLY Ruby stdlib (openssl, uri).
# rqrcode is optional – adds ASCII QR code display in the terminal.
gem 'rqrcode', '~> 2.2', require: false
